@import "sizes.styl"

.container
  display flex
  min-height "calc(100% - %s)" % headerHeight
  position relative
  & > .content
    flex 1
    position relative
    &::after
      content ''
      display block
      height musicPlayerHeight